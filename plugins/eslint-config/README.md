# @bloggerpack/eslint-config

> Standard eslint config for Bloggerpack.

## Installation

```bash
$ npm install @bloggerpack/eslint-config --save-dev
```

## Usage

`.eslintrc.json`:

```json
{
  "extends": "@bloggerpack/eslint-config"
}
```
