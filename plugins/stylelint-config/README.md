# @bloggerpack/stylelint-config

> Standard stylelint config for Bloggerpack.

## Installation

```bash
$ npm install @bloggerpack/stylelint-config --save-dev
```

## Usage

`.stylelintrc.json`:

```json
{
  "extends": "@bloggerpack/stylelint-config"
}
```
